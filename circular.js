const math = [
    'sucks', 'really sucks', 'dreadful', 'hate algorithms',
    'sucks', 'really sucks', 'dreadful', 'hate algorithms',
    'sucks', 'really sucks', 'dreadful', 'hate algorithms',
    'sucks', 'really sucks', 'dreadful', 'hate algorithms',
    'sucks', 'really sucks', 'dreadful', 'hate algorithms'];

const circleArray = (array, numberOfLoops) => {
let i = 0;
while(i < (array.length * numberOfLoops) {
console.log(i);
}
};